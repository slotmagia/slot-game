# HTTP Server
server:
  address:     ":8080"
  openapiPath: "/api.json"
  swaggerPath: "/swagger"

# Logger configurations
logger:
  level : "all"
  stdout: true

# Database configurations
database:
  logger:
    level:   "all"
    stdout:  true